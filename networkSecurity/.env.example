# Copy this file to .env and update the URI to match your MongoDB deployment
# For a local MongoDB server:
MONGO_DB_URI=mongodb://localhost:27017

# For MongoDB Atlas or a remote deployment, use the connection string provided by the service.
# Example:
# MONGO_DB_URI=mongodb+srv://user:password@cluster0.example.mongodb.net/?retryWrites=true&w=majority
